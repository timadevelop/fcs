defmodule Fcs.APITest do
  use ExUnit.Case
  # doctest Fcs.API

  test "find test" do
    # {:ok, result_map}= Fcs.API.find("do", "./test")
    # IO.inspect(result_map)
    # assert Fcs.API.find("some", ".") == :world
  end
end
